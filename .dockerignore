# Git files
.git
.gitignore

# Node modules (handled by Docker multi-stage build)
node_modules

# Next.js build output (handled by Docker multi-stage build)
.next

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript build info
*.tsbuildinfo

# Docker specific files (not needed inside the image)
Dockerfile
.dockerignore

# Project specific files/folders not needed in the final image
README.md
hosting/
apphosting.yaml
components.json

# Environment files (should be passed as environment variables to the container)
.env
.env.local
.env.*.local
.env.*
